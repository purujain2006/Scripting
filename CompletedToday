Lists Possibly Malicious Services
Made files: (still need to add to script)

  - greeter.dconf-defaults
  - custom.conf
  - logrotate.conf
  - ufw.conf
  - /etc/hosts
  - sysctl.d directory
 
Manual /etc/resolv.conf inspection
/var/www/html                      
Symlinks Script (Incorporate into script)
